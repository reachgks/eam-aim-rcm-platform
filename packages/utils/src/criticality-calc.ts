export interface CriticalityInput { safety: number; environmental: number; production: number; quality: number; maintenance: number; }
export function calculateCriticality(input: CriticalityInput): { score: number; grade: 'A' | 'B' | 'C' | 'D' } { const weights = { safety: 0.35, environmental: 0.25, production: 0.20, quality: 0.10, maintenance: 0.10 }; const score = Object.entries(weights).reduce((sum, [key, w]) => sum + (input[key as keyof CriticalityInput] || 0) * w, 0); const grade = score >= 80 ? 'A' : score >= 60 ? 'B' : score >= 40 ? 'C' : 'D'; return { score, grade }; }
